# WebView 2.0.10 hotfix.3 · 指针与计时排版

- 真正的表心分针/秒针取代外围标记；刻度与读数分离，停止或后台不产生虚假进度。
- 提醒页新增当前时钟/倒计时共用表盘；番茄页接入相同针引擎。
- 减少动态效果只停插值和装饰，功能性时钟仍按秒更新。
- 短屏读数与钟面并排，开始/重置按钮对齐；统计日期单行、月历标题精简。
- 原数据键/计时核心和图片不变；483逻辑、199离线浏览器检查通过。真机/120Hz/覆盖安装不在本轮验收范围。

---

# 2.0.10 hotfix.2 · 2026-09-18

- 修复拖尺被中断后误启动及页面指针残留；暂停倒计时同步已保存分钟。
- 修复弹窗返回被旧设置子页状态吞掉。
- 天气按前台/在线资格请求、按城市日期标注缓存，拒绝畸形或重复数据。
- 长会话目录改为单次整理，面板关闭不扫描全量历史。
- 修复恢复成功后的只读状态，阻止并发恢复覆盖。
- Android versionCode902062；只做本地交付，不发布正式版。

# WebView 2.0.10 hotfix.1（902061）

- 修复天气固定温度高度造成大字号干涉；文字、插画和控件分区，七日预报适配宽度。
- 修复零科目时开始按钮不可用，改为明确的添加并开始操作。
- 修复滑页进行中焦点滚动附加scrollLeft，导致整页和触控位置偏移。
- 修复SVG fill-box旋转中心，秒针在整圈和跨分钟保持正确轨道；隐藏/恢复动画尊重系统设置。
- 423项逻辑/源码、128项离线浏览器检查通过。按48组计时真实触摸、96组天气布局及完整原回归复测。
- 完整源码与独立签名验证APK；非原包覆盖升级，非真机/120Hz保证。未改主线/Compose分支。

---

# WebView 2.0.10 功能修订

- 会话目录、独立草稿、流式归属、原文索引保留与自动记忆/推荐联动。
- Skills本地MD/ZIP与公开GitHub导入、启用校验、引用资料、卸载与导出。
- Motion跟手页/底栏、共享照片、可中断弹簧，天气场景与刻度计时器/拖尺。
- 双方独立聊天外观、95%/13px默认、用户透明素材、低干扰玻璃/悬浮输入。
- 修复滑页次序、读后感丢内容、玻璃恢复失败、空白会话重命名和Markdown导出MIME。
- 415逻辑检查、89离线浏览器检查及Android打包/签名通过；非真机全量验收。
- 本轮为本地完整源码交付，未写GitHub；不是已取回原始完整2.0.9的声明。

---

# Changelog

## v2.0.0（2026-09-13）
- 新增歌曲与书评独立聊天记录查看与关键词搜索，记录按话题保存在本机。
- 统一随身歌单与书评卡片底部按钮布局、间距和高度。
- 调整独立聊天输入框与发送按钮为紧凑同高布局。
- 更新 Android、npm 与构建脚本版本号至 2.0.0（versionCode 902000）。
- 保持原应用包名与签名，支持覆盖安装并保留本地数据。

# CHANGELOG

## v1.10.0（2026-09-12）
- 统一四季与昼夜主题变量，聊天、推荐、阅读、手记和设置文字跟随当前主题。
- 增加页面错误兜底，修正设置开关无障碍状态，并整理移动端检查脚本路径。
- 保留现有业务逻辑与本地存档格式。

## v1.9.6（2026-09-12）
- 情景模拟、音乐评价、书评使用独立对话窗口与独立存档，不打断悄悄话。
- 统一“他的此刻”相关按钮尺寸，键盘弹出时独立对话自适应。
- 生产构建已运行；类型检查仍受现有历史错误阻塞。


## v1.9.5 正式版（2026-09-11）
- 新增：`lib/daily-picks.ts` 每日推荐模块，用内置模型 API 生成当天内容，并按日期缓存在本地（存储键 `luke-daily-picks-v1`），同一天不重复请求。
- 新增：`components/daily-picks.tsx` 提供 `useDailyPicks`，在页面级调用一次；打开应用时若缓存不是今天的就自动更新。
- 修改：「夏彦的随身歌单」改用今日歌单（8 到 10 首，标题显示「今日 · N 首」），「换一首」在今日歌单内切换，并新增刷新按钮。
- 修改：「最近在读」把今日推荐的书排在书单最前并标注「今日推荐」，原有内置书目、用户添加的书籍和「添加书籍」流程不变，并新增刷新按钮。
- 兜底：未配置模型或请求失败时回退到内置的本地内容，失败原因显示在歌单下方，界面不会空白。
- 版本号：Android 版本号升级为 1.9.5（versionCode 901005），构建脚本输出名同步为 `Four-Seasons-Luke-1.9.5.apk`，`package.json` 同步为 1.9.5。

## v1.9.4 正式版（2026-09-11）
- 启动流程：移除启动遮罩与开屏编排，`components/startup.tsx` 大幅精简，`app/layout.tsx` 不再引入 `startup-splash.css` / `startup-reveal.css` / `startup-nav.css`，进入应用后直接显示主界面与底部导航。
- 时光手记：编辑器、新建按钮与笔记本浮层改用 Portal 渲染到 `document.body`，并按 `visualViewport` 动态调整编辑器高度，修复移动端键盘弹出时的遮挡与错位。
- 样式统一：调整 `app/notes-polish.css` 与 `components/memo-board.module.css`，统一时光手记与聊天输入区（composer）的对齐与高度。
- 新增模块：`lib/agent/` 提供夏彦人格（`constitution` / `persona-builder` / `style` / `situations`）与长期记忆（`memory/store` / `memory/types`）基础代码。该模块目前为独立代码，尚未接入界面。
- 版本号：Android 版本号升级为 1.9.4（versionCode 901004），构建脚本输出名同步为 `Four-Seasons-Luke-1.9.4.apk`。
- 版本说明：1.9.3 为本地中间构建，未单独发布，其改动已并入 1.9.4；versionCode 由 901002 直接递增到 901004。
- 发布状态：本版本尚未提交到 git 仓库，仓库最高 tag 仍为 `android-four-seasons-v1.9.2`。

## v1.9.2 正式版（2026-09-11）
- 正式发布：将已完成真机验证的 `dev/local-work` 稳定版本合入 `main`，正式基线为提交 `03c1109dd76843917dda7f3c84e2571cb889bf5c`。
- 修复：移除启动遮罩/开屏编排造成的等待与卡顿，应用进入后直接显示主界面与底部导航。
- 优化：统一页面、弹窗、按钮与局部内容的交互节奏，避免瞬间出现/消失和不必要的复杂动效。
- 修复：季节/昼夜主题切换改为预加载后原子切换，消除主题切换时的闪屏、颜色过渡错位和旧画面残留。
- 修复：四季相册在图片解码完成后再切换，快速连续切换时以最新请求为准，避免空白帧与卡顿。
- 修改：时光手记配色统一使用季节与昼夜主题变量，修复搜索框、标签、卡片、编辑器及按钮固定配色问题。
- 修改：Android 版本号升级为 1.9.2（versionCode 901002），正式源码包名保持 `com.luke.summer`。
- 发布规则：GitHub Release 统一按版本号命名；每次新版本发布时标记为 Latest，Release 页面按发布时间让最新版本位于前面，历史版本依次保留用于追溯。
- 数据说明：本版本不主动清除本地存储。仍建议升级前从应用内导出完整备份；后续 Android APK 必须继续使用同一 release signing key，避免因签名或包身份变化产生新的数据空间。

## v1.9.1 正式版（2026-09-10）
- 新增：内置夏彦人物记忆库，共 412 条结构化知识，覆盖人物基础 41 条、性格习惯 76 条、人物关系 38 条、能力兴趣 42 条、重要经历 155 条、日常细节 60 条。
- 新增：聊天时根据最近用户问题动态检索相关夏彦人物知识，只注入与当前话题相关的少量条目，不把 412 条全部塞入提示词。
- 新增：人物知识保留 A / A2 / B / C 来源可信度，并在冲突时优先使用更高可信度资料。
- 新增：主线现实与“换日线”等平行/AU、梦境/特殊世界线隔离；普通聊天不会把特殊世界线误写成主线经历。
- 修改：夏彦原作人物知识与用户长期聊天记忆保持分离，原作“女主/两人”相关事实默认只作为角色背景与行为风格参考，不自动改写成当前用户亲历。
- 修改：Android 版本号升级为 1.9.1（versionCode 901001）。
- 升级兼容：1.9.1 正式包继续沿用 1.9.0 正式包的 Android 安装身份与同一签名证书，并使用更高 versionCode，可直接覆盖安装 1.9.0；本次未修改聊天、计划、笔记、长期记忆、完整备份等本地存储结构和存储键。
- 发布校验：正式构建必须通过 412 条知识数量/检索/AU 隔离测试、移动 Web 构建、APK 签名与 zipalign 校验，并与已发布 1.9.0 APK 比较包名、签名证书和 versionCode；任一不一致则拒绝发布。
- 备份建议：Android 原地升级按设计会保留应用数据，但设备、系统或文件损坏等外部因素无法由安装包完全排除，升级前仍建议在“设置 → 日常与数据”导出一次软件完整备份。

## v1.9.0 正式版（2026-09-10）
- 正式发布：将近期验证完成的统一笔记、聊天附件、完整备份、四季昼夜联动与开屏体验纳入 Android 正式版本。
- 新增：将“手记/备忘”统一为一个笔记系统，支持笔记本、置顶、加星、搜索、心情标签、回收站与本地持久化。
- 新增：「悄悄话」支持图片与文件附件；图片可随本轮发送给支持视觉的兼容模型，TXT、Markdown、JSON、CSV、代码等文本文件可读取正文后随消息发送。
- 新增：软件完整备份与恢复，覆盖聊天、计划、统一笔记、笔记本、学习/阅读记录、纪念日、外观及非敏感连接设置；API Key、天气 Token 等敏感凭据不写入备份。
- 新增：四季与昼夜联动开屏动画，采用揭幕式进入主页；移动端底部导航在主页稳定后独立延后出现，避免启动阶段突兀跳出。
- 修改：聊天输入区的附件键与发送键合并为同一右侧操作区；附件、发送按钮及附件卡片跟随春夏秋冬与昼夜主题变化。
- 修改：各页面专属操作统一右上角对齐；全局设置仅保留首页/侧栏入口，移除其他页面重复设置按钮。
- 修改：时光手记页面仅保留应用级顶栏，移除重复的 NOTES/笔记二级顶栏。
- 升级兼容：1.9.0 Android 正式安装包延续最近测试版 v12 的应用包身份与同一签名证书，并使用更高 versionCode，因此 v12 可直接覆盖安装，Android 会保留原应用数据，无需卸载。
- 备份建议：虽然原地升级不会主动清空数据，正式升级前仍建议在“设置 → 日常与数据”导出一次软件完整备份，以防设备或系统级异常。

## v0.7（2026-09-10）
- 新增：将“手记/备忘”统一为笔记系统，支持笔记本、置顶、加星、搜索、心情标签、回收站与本地持久化。
- 新增：「悄悄话」输入区增加图片/文件附件入口；图片可随本轮发送给支持视觉的兼容模型，文本文件可读取正文后随消息发送。
- 新增：软件完整备份与恢复，覆盖聊天、计划、统一笔记、笔记本、学习/阅读记录、纪念日、外观及非敏感连接设置；API Key、Token 等敏感凭据不进入备份。
- 修改：各页面专属操作统一靠右对齐；全局设置仅保留首页/侧栏入口，移除其他页面重复设置按钮。
- 修改：时光手记页面仅保留应用级顶栏，移除重复的 NOTES/笔记二级顶栏。
- 修复：提高 Android 附件请求与完整备份导出的受控容量上限，兼顾附件功能与本地数据安全边界。

## v0.6（2026-09-10）
- 新增：中文项目 README，加入项目介绍、使用方式、目录说明和角色/四季图片。
- 修改：将说明文档移动到 `docs/`，将检查脚本移动到 `scripts/checks/`，同步更新文档中的命令路径。
- 修复：减少根目录文件堆叠，改善 GitHub 首页的浏览和下载指引。

## v0.1（2026-09-09）
- 提交：`3cc5864`
- 说明：Build Luke companion website with local records and backups
- 新增：
  - 初始化前端项目结构（`app/`、`components/`、`lib/`、`hooks/`）和基础样式。
  - 引入 UI 组件库、Lint/构建配置和主要前端依赖。
  - 新增配置与校验文件（`.openai/hosting.json`、`components.json`、`VERIFICATION.md`、`next.config.ts` 等）。
- 修改：
  - 建立初始页面骨架和主入口流程。
  - 增加基础样式与入口布局。
- 修复：
  - 完成首版可运行结构与基础项目可构建状态。

## v0.2（2026-09-09）
- 提交：`4f37960`
- 说明：Connect Caiyun weather and model chat with persistent local Luke memory
- 新增：
  - 集成 Caiyun天气服务代理接口与模型代理接口。
  - 新增 `CHARACTER_AND_API.md`，补充模型/天气接口说明。
  - 增加会话记忆持久化与模型/天气配置字段。
- 修改：
  - 扩展连接页与配置路径，完善验证流程。
  - 引入天气与模型相关工具函数。
  - 调整 `app/page.tsx` 与 `tsconfig` 配置以支持新功能。
- 修复：
  - 对输入参数（URL、位点、Token）增加合法性校验，降低无效配置风险。

## v0.3（2026-09-09）
- 提交：`e985508`
- 说明：Feature Luke artwork and direct chat API configuration
- 新增：
  - 新增艺术素材（`public/images/*`）与视觉展示资源。
  - 增加直接聊天 API 配置能力并增加对应检查。
- 修改：
  - `app/page.tsx` 与 `components/connections.tsx` 增强配置流程。
  - `components/` 与 `lib/` 中对模型连接与界面交互进行适配。
- 修复：
  - 调整聊天配置与展示逻辑，减少无效请求路径和界面异常。

## v0.4（2026-09-10）
- 提交：`76504d7`
- 说明：Publish for GitHub
- 新增：
  - 增加多处核心陪伴功能模块：四季体验、音乐、学习、专注、计划、手记、待办、倒计时等。
  - 新增移动端构建与打包入口（`mobile/` 相关文件）。
  - 引入更完整的业务组件与页面状态管理。
  - 扩展 `lib/` 业务逻辑（专注、学习、音乐、季节、位置、流式模型等）。
- 修改：
  - 优化主页面与样式，完善交互流程与状态显示。
  - 改进模型与天气请求链路在桌面与移动端的兼容处理。
- 修复：
  - 补齐更多边界校验与异常分支。

## v0.5（2026-09-10）
- 提交：`25bfda7`
- 说明：Prepare repository for GitHub release
- 新增：
  - 增加发布前的检查脚本集合（`check-*.mjs`）。
  - 补充 `.gitignore`，增强本地构建产物/缓存/敏感文件过滤。
  - 补充备份/历史相关图片与资源归档。
- 修改：
  - 完善发布前校验覆盖面（模型、天气、交互、季节、移动端体验等）。
- 修复：
  - 收口发布节奏相关问题，确保仓库内容更适合正式托管和持续维护。

