# 2.0.10 原生迁移状态

本文件记录实际完成状态，不能把工程目录或接口存在解释为完整应用已经迁移。

## 当前阶段

原生基础工程及数据／业务层检查点。独立目录为 `native/`，目标应用包名 `cn.sishiyuni.nativeapp`。这个检查点尚无可用的完整原生界面，不能替代 2.0.9，不发布 APK 成品或正式 Release。旧 React/Android 工程留在父目录用于比对，不属于原生模块依赖。

原生设计系统和界面的批量写入请求未成功落库；当前未提交的 Shared Element、通透背景、动态 Tab 等界面代码不计入完成项。本轮独立完成的业务修复详见 `STATE-SAFETY.md`。测试结果必须对应具体代码提交；旧 `CORE-VERIFICATION.md` 只记载第一阶段 20 个测试，不代表当前全部测试。

## 架构与数据

| 项目 | 当前状态 | 验收要求 |
| --- | --- | --- |
| app / core / designsystem / 七个 feature 模块 | Gradle 结构已提交 | 完整 app 编译、安装、启动通过后才算运行工程 |
| Room / DAO / StateFlow | 实体、查询和业务持久化已提交；新增真实 Android 模拟器 Room 仪器测试，修正消息冲突被静默忽略的问题 | 对应提交的仪器测试全部通过后，继续验证磁盘持久化、重启、空间不足及旧备份 |
| DataStore | 默认 95% / 13，范围 75–160% / 10–32 已提交 | 原生设置界面、系统字体放大及持久化复测 |
| 模型密钥 | AndroidKeyStore 加密、绑定服务地址代码已提交 | 真机保存／更换地址／密钥删除验证；不随备份导出 |
| MVVM | CoreViewModel 基类已提交 | 各 feature ViewModel 与 Compose 状态收集尚未接入 |
| WebView 移除 | native 新源码未引入 WebView | 完整构建后扫描 DEX 与 APK 资产，确认无 WebView、React、HTML/JS 运行时 |
| 旧数据导入 | 完整备份和旧主存档解析、事务导入、保留原文、设置恢复日志已提交；测试事务失败回滚 | 以实际 2.0.9 备份进行条数、内容、关联、显示和往返导出核对 |

## 逐功能迁移

| 功能 | 已写入的原生代码 | 尚需完成 |
| --- | --- | --- |
| 四季／昼夜与透明滚动 | 设置模型 | 原生配色、磨砂渐隐层、悬浮输入区、系统栏适配、开关与降低透明度回退 |
| 跟手滑页／动态 Tab | 模块规划 | HorizontalPager、随偏移连续移动的指示器、子滚动冲突与快速反向操作 |
| Shared Element／Spring | 依赖已解析 | 列表到详情实际共享元素、可中断弹簧、系统减少动画适配 |
| 粒子／高刷新率 | 暂无界面实现 | 帧时钟驱动的局部绘制、离屏停机、功耗约束、真机帧时间统计 |
| 回到身边 | 通用记录表及旧原文保留 | 相伴／今日／约定／回顾全部界面与操作、全部原图片资源比对 |
| 天气 | Open-Meteo 搜索、城市缓存；隔离迟到响应，校验预报，区分网络与缓存失败 | 天气动画、预报展示、城市选择／定位、真实连通性及署名安排 |
| 天气参考仓库接口 | 未复制 Xiaomi 固定客户端标识／签名 | 核实适用条件后决定数据源，不能把“参考仓库内置”写成通用免费接口保证 |
| 悄悄话 | 会话、消息、草稿、检索、归档、更名、流式回复绑定、取消、备用模型；原子元数据更新，连续上下文窗口和消息身份保护 | 原生聊天列表、右上角对话工具、新建／历史操作闭环、附件与重新生成等全量比对 |
| 头像／气泡 | 独立设置字段、SAF 头像导入与像素压缩 | 原生候选与相册界面、方向处理、皮肤绘制、头像与气泡对齐、多字号边界 |
| 自动长期记忆 | WorkManager 调度、事实来源、指纹、遗忘标记与人工固定；移除每条 1600 字符截断，按完整消息和真实 JSON 大小分批，旧摘要指纹版本失效重建 | 单条超出请求上限的历史长文断点分片、旧索引全量迁移、真实模型质量与长期运行、设置界面 |
| Skills | GitHub／SKILL.md／ZIP 检查、默认停用、启用／卸载、聊天注入；审阅摘要校验更新，防止迟到操作复活已卸载技能，ZIP 路径与同名冲突校验 | 原生管理界面及审阅确认接入、取消与失败恢复；仅指令技能，不运行任意脚本 |
| 他的此刻 | 动态推荐、明确偏好、实际收藏与负反馈、书架记录代码 | 原生音乐／书籍／历史／收藏／评价界面，旧收藏和历史的完整映射 |
| 一起计划 | Plan Entity、CRUD 与年度节假日仓库 | 月历、跳转、休班标记、原“今天”头像、全部计划和约定操作 |
| 节假日 | 本地 JSON 优先、静态更新、缺年时 Timor、校验及退避代码 | 把已核对的年度文件和许可加入原生资产；在线和离线 UI 验证 |
| 时光手记 | 笔记／文件夹／回收站字段、乐观版本更新、导出代码；数据库测试旧版本拒绝覆盖及文件夹删除保留笔记 | 原生编辑、格式选区、自动保存、失败保留草稿、多选、搜索、分页、共享元素 |
| 学习正计时 | 单调时间、跨日记录、一次保存；暂停状态防覆盖、重复开始／暂停幂等，明确丢弃后才能重置 | 原生表盘、科目管理、统计、月历、完整崩溃恢复与真实手机验证 |
| 拉尺倒计时 | 阻尼／选时数学、单调期限、AlarmManager 与通知；替换确认和早到事件重排 | 跟手滑尺、松手／取消／越界反馈、运行中操作、前后台和真实手机权限测试 |
| 原有番茄钟等 | 旧原文保留 | 不能以原文保留替代迁移；工作／短休／长休／轮数／常用任务逐项补齐 |
| 设置 | 设置仓库、备份、密钥接口 | 所有分组、搜索、天气、日历、隐私、记忆、免责声明和错误交互 |

## 检查范围

CI `Native migration checkpoint 210` 编译和执行 `:core:testDebugUnitTest`；`Native Room persistence 210` 在 Android API 30 x86_64 模拟器执行 `:core:connectedDebugAndroidTest`。后者不是 JVM 平台桩，但也不是实际手机，更不是完整应用 UI 测试。实际用例总数、失败数以该提交对应的报告为准；不能把添加测试等同于测试通过。

尚未完成完整 Compose UI 测试、旧版实际备份往返迁移、完整 APK 编译或真机帧率验收。测试通过也不能据此声称“所有功能无回退”或“120Hz 绝不卡顿”。原生阶段不合并 main，旧应用保持可用。所有迁移验收项完成后，再选择覆盖升级或独立安装方案，并核对签名与数据导入路径。
