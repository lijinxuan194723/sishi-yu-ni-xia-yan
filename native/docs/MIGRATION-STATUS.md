# 2.0.10 原生迁移状态

更新于 2026-09-16。本文件区分“已提交的原生代码”“已验证的组件”和“完整产品功能”，不以目录、测试宿主或数据原文保留替代全功能迁移。

## 当前阶段

迁移分支：`feature/v2.0.10-native-migration`。原生代码位于 `native/`；目标应用包名 `cn.sishiyuni.nativeapp`。旧 React/Android 工程暂留在父目录用于功能、数据与资源比对，不作为原生模块依赖。

当前已有真实 Compose 组件及若干功能界面，不再只有数据层。但完整生产 `:app` 的页面整合、入口启动与全部功能尚未完成。`uitesthost` 是 Android 仪器测试宿主，不能作为 2.0.9 替代品或正式原生 APK 发布。

最新已经核对日志的生产代码检查点：`7cac1152ff4d2d615bca6ff8c2870a90571ede37`。后续 `794d6816` 仅新增节假日验证脚本／工作流，未改原生生产代码。详细运行、失败边界、报告与源码快照见 `LAYOUT-PERSISTENCE-VERIFICATION.md`。

## 逐项状态

| 需求 | 已提交并接入或可调用的部分 | 仍需完成 |
| --- | --- | --- |
| Kotlin / MVVM / StateFlow / Room / DataStore | 独立模块、仓库、实体、状态收集；首页／聊天／计时／Skills 部分 ViewModel 与 Compose 已接入 | 全部 feature 的生产入口、完整操作闭环、磁盘崩溃恢复和真实旧备份核对 |
| WebView 移除 | 新的原生模块以 Kotlin/Compose 实现，不依赖旧 React 运行时 | 完整应用打包后扫描 DEX、依赖及资产；不能仅以新目录不存在 WebView 认定全产品已经移除 |
| 四季 / 昼夜 / 字号 | 原生主题；全局默认 95%、聊天 13，范围 75–160% / 10–32 | 完整设置入口、季节自动变化时机、全页面与系统栏复核 |
| 跟手滑页 / 动态 Tab / Spring | 真实 Pager 偏移驱动指示器，可反向及中断，减少动画回退；模拟器测试保留 | 整个生产导航、所有内外滑动冲突、实体手机帧率 |
| Shared Element | 首页照片与详情共享元素、同源图片缓存与返回状态 | 全部需要共享过渡的详情、不同主题切换中的覆盖层验证 |
| 通透滚动 | 原生录制层、磨砂／渐隐边缘；首页新接入 LayeredContent，内容可真正滑到分区栏背后 | 手记、计划及完整设置尚未统一接入；全应用底部悬浮与输入法验证 |
| 回到身边 | 相伴／今日／约定／回顾页面；照片由内容决定高度、标签正文不叠放；相伴天数和七天统计修正 | 与旧版全部按钮、图片与设置入口的逐项比对 |
| 天气 | Open-Meteo 城市检索、缓存与竞态保护、预报界面、Canvas 天气粒子；离屏停止绘制 | 定位、真实公共接口连通性、完整设置与署名安排；未复制第三方 Xiaomi 固定签名 |
| 计时 / 拉尺提醒 | 机械刻度盘、外圈扫针、数字安全区、计时操作、拉尺释放启动与取消保护 | 原有番茄钟工作／短休／长休／轮数／常用任务等全量功能；真实闹钟权限与后台存活 |
| 多会话聊天 | 新建／历史／更名／归档／检索／加星／更正与会话绑定流式回复，应用级草稿 | 全部附件、重新生成、完整皮肤候选、全部历史存档与新会话备份映射 |
| 头像 / 气泡 | 双方独立设置、相册像素处理、原生行对齐和透明资源显示 | 旧有全部装饰气泡及每个字号组合；真实图库、EXIF 与迁移路径复核 |
| 自动长期记忆 | WorkManager 调度、原文证据、摘要指纹、遗忘标记、完整消息分批 | 超大单条消息内部断点分片、旧索引全量迁移、实际模型长期质量与设置入口 |
| Skills | 原生管理与导入审阅、默认停用、摘要核验、确认启用／更新／卸载；仅指令技能 | 真实手机文件选择和网络失败恢复全链路；不执行任意脚本或授予系统权限 |
| 一起计划 | 新增事务仓库，修改正文保留并发完成／重要状态，过期确认拒绝、撤销删除 | 完整 PlansScreen 与 PlansViewModel 本轮写入被工具安全检查阻止，未提交；不能计为完整计划页面 |
| 日历 / 休班 / 今天头像 | NativeCalendar 组件、原 cat.webp 替换今天数字、独立休班标记带、测量字形宽度、六周网格、严格日期和月份边界 | 在完整计划页面接入跳转、月切换和所有事件类型；真实设备可读性验证 |
| 节假日两套方案 | 仓库保留本地静态优先、静态更新与缺年时 Timor 备用；2023–2026 JSON 和 MIT 许可已放入 native/assets；实际联网对照固定上游成功 | 生产 app 的资产接入与完整日历设置页、未来年份、Timor 公共接口实时验证 |
| 时光手记 | 应用级编辑会话、合并输入、修订检查、恢复记录、格式选区算法、文件夹／星标／置顶／回收站仓库及真实 Room 测试 | 完整 JournalScreen、JournalViewModel 与原生编辑弹层本轮写入被安全检查阻止，未提交；工具栏、搜索、多选、分页、导出等 UI 闭环尚未完成 |
| 他的此刻 | 个性化推荐与反馈、书架业务层 | 音乐／共读／收藏／历史／评价完整原生页面及旧数据映射 |
| 完整设置 / 备份 | 设置仓库、密钥加密、备份导入导出接口；Skills 管理界面 | 搜索与所有设置分组、密钥表单、日历天气隐私说明、失败交互、真实备份往返迁移 |
| 120Hz 与能耗 | 局部 Canvas／绘制层状态、可暂停帧时钟、减少动态效果 | 实体高刷手机 FrameMetrics / Macrobenchmark / 功耗验收；不承诺恒定120fps或绝无卡顿 |

## 本轮实际检查

- 核心 JVM：104 项通过，0 失败、0 错误、0 跳过。
- Compose 仪器测试：Android API 30 / 35 各 34 项通过；同一用例集在两系统分别执行。
- Android API 30 Room：29 项通过，使用实际 Room/SQLite 内存数据库。
- 日历组件包含 27 组宽度／应用缩放／系统字号的边界检查。
- 透明分区栏检查实际像素变化、固定控件点击与末项避让，不只断言状态变量。
- 四个本地年度文件与固定上游完整 JSON 等价，保留全部日期、标志、名称、公告引用与许可；不声称空白排版也逐字节一致。

测试命令分别是 `:uitesthost:assembleDebug :uitesthost:assembleDebugAndroidTest :core:testDebugUnitTest`、`:uitesthost:connectedDebugAndroidTest`、`:core:connectedDebugAndroidTest`，不是完整生产应用测试。

## 安全边界与后续验收

本轮手记及计划完整 UI 两批上传被工具安全检查阻止，已经停止该批写入，没有换路径或编码绕过。独立的首页、日历组件、业务仓库、数据文件和测试已成功提交；这些成功项不能代替被阻止的界面。

尚未验证真机 120Hz、真实输入法／相册、完整应用启动、通知权限、系统结束进程、磁盘空间不足或掉电、旧2.0.9全量备份往返、真实模型输出质量。数据库写入失败时当前进程保留草稿，不意味着没有写盘的文字能在断电后恢复。

维持独立迁移分支，不合并 main，不发布正式 Release，不把阶段测试宿主当作成品 APK。
